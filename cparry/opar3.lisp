;; this file is based on opar3.lap, no mlisp source exists
;; Almost I can't understand well.

(defun choose (x)
  (nyi)
  (return x)
;  (unless (equal x NIL) 
;    (setf CHOSEN (get CHOSEN 'IND)) ; setf is correct?
;    (unless CHOSEN 
;      ;go TAG6
;    (if CHOSEN (EQUAL EXHAUST CHOSEN ) 
;      (go TAG10))
;   (if (choose t ENDE 'BYEFEDUP) ;; order may be reverse
;     (go TAG7))
;   ; tag10
;   (choose 'EXHAUST)
;   ; tag11, tag7
;   (go TAG1)
;   ;tag 6
;   (if (equal ...) (got tag1))
;   ; tag2
;    (setf y nil)
;   ;tag1
;   )
;  )
)

(formta t "end of loading opar3.lisp")

   


